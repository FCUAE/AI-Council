Act as a Senior DevSecOps and QA Engineer. We need to audit, clean, secure, and stress-test this codebase without breaking any existing functionality. 

Please execute this process strictly in the following phases. **Do not modify or delete any code yet.** First, analyze the workspace and provide a report or generate the necessary testing scripts. Wait for my approval before moving to the next phase.

**Phase 1: Code Cleanup & Dead Code Elimination**
1. Scan the codebase for unused imports, uncalled functions, unused variables, and orphaned files. 
2. Check for unused dependencies in the package manager file (e.g., package.json, requirements.txt).
3. Generate a detailed list of what you found. Do not delete anything yet. If appropriate, write a short shell script utilizing a linter (like ESLint for JS/TS or Pylint/Vulture for Python) to help identify dead code.

**Phase 2: Security Vulnerability Audit**
1. Analyze the codebase for common OWASP vulnerabilities (e.g., hardcoded secrets, SQL injection risks, insecure API endpoints).
2. Check dependencies for known CVEs. Write and execute the appropriate audit command for my stack (e.g., `npm audit`, `pip-audit`, or `safety`).
3. Provide a summary report of high and critical security risks and propose specific code changes to patch them.

**Phase 3: Stress / Load Testing Setup**
1. Review the primary API endpoints or server routes in this project.
2. Write a load-testing script using a standard tool suitable for this environment (e.g., a simple Python script using `locust`, or a Node.js script using `autocannon` or `k6`).
3. Ensure the script tests for high concurrency (e.g., 100+ simultaneous connections) and measures response times and error rates. Provide instructions on how I should run this script.

**Phase 4: Regression Safety & Verification**
1. Before we apply any cleanup or security patches, check if there is an existing test suite. 
2. If there are no tests, generate a basic suite of unit/integration tests for the most critical endpoints/functions to ensure they return a 200 OK status. 
3. Outline a safe deployment plan to ensure that when we do remove the dead code and apply security patches, we can easily revert if something breaks.

Please begin with Phase 1. Analyze the language and framework of this workspace and tell me what you find regarding dead code.