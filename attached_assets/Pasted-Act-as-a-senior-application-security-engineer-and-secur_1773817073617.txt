Act as a senior application security engineer and secure code reviewer. Your job is to perform a full security audit of this entire project, identify every realistic security weakness, fix what can be fixed safely, and clearly report what you changed.

Scope:
Review the entire codebase, configuration, infrastructure-related files, dependencies, environment variable usage, auth flows, API endpoints, database access patterns, file uploads, external integrations, client-side code, server-side code, and deployment settings.

Your objectives:
1. Find security vulnerabilities
2. Fix them directly where safe and low risk
3. Harden the application against common attack paths
4. Preserve app functionality
5. Explain all findings, fixes, and any remaining risks

Follow this exact security review checklist:

A. Secrets and sensitive data
- Search the entire project for hardcoded secrets, API keys, tokens, passwords, private URLs, credentials, signing keys, and connection strings
- Move secrets to environment variables if needed
- Ensure secrets are never exposed to frontend bundles, logs, client responses, or public files
- Check that .env files, secret files, build outputs, and generated artifacts are properly ignored by git
- Verify no sensitive values are committed in config files, source code, test files, fixtures, or documentation
- Flag any secrets that cannot be rotated automatically

B. Authentication and authorization
- Review login, signup, logout, password reset, email verification, session handling, token issuance, refresh tokens, magic links, OAuth flows, and role-based access
- Ensure authentication checks cannot be bypassed
- Ensure every protected route, API endpoint, admin page, background job, and privileged action enforces authorization server-side
- Check for insecure direct object reference issues, broken access control, privilege escalation, and missing tenant isolation
- Verify users can only access their own data unless explicitly allowed
- Confirm admin-only functionality is actually restricted on the backend, not just hidden in the UI

C. Session and token security
- Verify cookies use HttpOnly, Secure, SameSite where appropriate
- Check token expiration, refresh logic, revocation, replay resistance, and storage safety
- Ensure tokens are not stored insecurely in localStorage unless absolutely necessary and justified
- Review JWT or session signing configuration for weak secrets, unsafe algorithms, or missing validation
- Ensure session fixation risks are addressed
- Invalidate sessions on logout and sensitive account changes where possible

D. Input validation and output handling
- Review all request inputs, query params, path params, headers, form inputs, uploaded files, webhooks, and database inputs
- Add strict validation and sanitization where missing
- Prevent SQL injection, NoSQL injection, command injection, template injection, path traversal, header injection, and unsafe deserialization
- Prevent reflected, stored, and DOM-based XSS
- Ensure output encoding is context-aware
- Validate all file names, mime types, extensions, sizes, and storage paths for file uploads

E. API and backend security
- Audit every API route and server action for auth, authorization, validation, rate limiting, and safe error handling
- Ensure no internal stack traces, secrets, SQL errors, or sensitive debug details leak to clients
- Verify unsafe HTTP methods are restricted where appropriate
- Add protections against mass assignment and over-posting vulnerabilities
- Ensure webhooks verify signatures and reject untrusted origins where applicable
- Verify CORS is minimally permissive and not wildcarded unless absolutely required
- Check SSRF risks in any URL fetch, image proxy, import, scraper, webhook, or remote file feature

F. Database and data protection
- Review all database queries, ORM usage, raw SQL, migrations, and seed logic
- Ensure parameterized queries or safe ORM patterns are used everywhere
- Check row-level access patterns and multi-user data boundaries
- Flag overly broad queries that may leak data
- Ensure sensitive fields are not returned unnecessarily
- Check encryption or hashing for passwords and highly sensitive data
- Verify passwords use a modern password hashing algorithm such as bcrypt, argon2, or equivalent with safe parameters
- Ensure no plaintext passwords are stored or logged

G. Frontend and client security
- Check for exposed environment variables in the frontend
- Prevent XSS in rendered user content, markdown, HTML injection, dangerouslySetInnerHTML, template rendering, and URL handling
- Review local storage, session storage, and client state for sensitive data leakage
- Ensure forms and client calls do not expose internal implementation details
- Check unsafe redirects and open redirect risks
- Review CSP compatibility and suggest improvements

H. Dependency and supply chain security
- Audit all dependencies and lockfiles for known vulnerabilities, abandoned packages, suspicious packages, unnecessary packages, and overly broad version ranges
- Update dependencies safely where possible
- Remove unused risky dependencies if they are not needed
- Check postinstall scripts and custom scripts for risky behavior
- Flag packages with known severe CVEs or suspicious maintenance history

I. Security headers and browser protections
- Review and improve security headers including Content-Security-Policy, X-Frame-Options or frame-ancestors, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, Strict-Transport-Security where applicable
- Check CSRF protections for cookie-based auth and state-changing requests
- Ensure clickjacking protections are in place
- Check mixed content and insecure asset loading risks

J. File handling and storage
- Audit file upload, download, preview, parsing, import, export, and attachment flows
- Prevent executable file upload risks, path traversal, malicious SVG issues, oversized file abuse, and unsafe temp file handling
- Ensure uploaded files are stored outside executable paths where applicable
- Verify signed URLs or access controls for private file access

K. Logging, monitoring, and error handling
- Ensure logs do not contain secrets, tokens, personal data, passwords, or payment info
- Review error handling and observability hooks for leakage
- Ensure production mode disables debug behavior
- Replace unsafe console logging of sensitive objects
- Add safer structured logging if needed

L. Rate limiting, abuse, and denial of service
- Identify endpoints vulnerable to brute force, credential stuffing, spam, scraping, enumeration, and denial of service
- Add rate limiting, cooldowns, CAPTCHA hooks, pagination limits, request size limits, timeouts, and concurrency protections where relevant
- Prevent user enumeration in auth and recovery flows
- Check expensive endpoints and unbounded queries

M. Infrastructure and configuration
- Review Dockerfiles, deployment configs, CI/CD configs, GitHub Actions, Replit settings, package scripts, build config, and server startup config
- Remove dangerous defaults
- Ensure production uses secure settings
- Check allowed hosts, trusted proxies, HTTPS assumptions, and environment separation
- Ensure dev-only tooling is not exposed in production
- Flag missing backup, rotation, or operational hardening issues if visible from the repo

N. Business logic security
- Look for workflow abuse, price tampering, coupon abuse, payment manipulation, race conditions, duplicate submissions, trust in client-side values, and privilege logic flaws
- Review any onboarding, purchases, credits, invites, referrals, admin tooling, and user-generated workflows for abuse paths
- Ensure important actions are validated server-side and idempotent where needed

O. Privacy and compliance hygiene
- Minimize collection and exposure of personal data
- Ensure sensitive user data is not unnecessarily returned to the client
- Flag retention or exposure risks that are visible in the codebase
- Check cookie and tracking behavior for obvious privacy issues

Execution rules:
- Be aggressive in finding issues, but conservative in making risky architectural changes
- Fix issues directly when the fix is clear and low risk
- Do not break existing features
- If a fix could break functionality, explain the issue and propose the safest patch before making a major change
- Prefer secure defaults
- Prefer server-side enforcement over client-side checks
- Prefer explicit allowlists over blocklists
- Add validation schemas where appropriate
- Tighten types if that improves security
- Remove dead insecure code where safe
- Keep code readable and maintainable

Testing requirements:
- After applying fixes, run the app and relevant tests
- Add or update tests for security-sensitive flows where feasible
- Verify auth, permissions, validation, and critical business logic still work
- Check that no secrets are printed during build or runtime
- Confirm the application still builds successfully

Deliverables:
1. A prioritized security audit summary with:
   - Critical
   - High
   - Medium
   - Low
2. For each finding include:
   - What the issue is
   - Where it exists
   - Why it matters
   - How you fixed it, or why it was not auto-fixed
3. A list of all files changed
4. Any follow-up recommendations that require product or architectural decisions
5. A final section called “Remaining Risks” for anything not fully resolved

Important:
- Do not give me a shallow summary
- Do not stop after checking only auth or only dependencies
- Review every major security angle in the project
- Think like both a defender and an attacker
- Assume this app may become production-facing and must be hardened accordingly
- If you find no issues in an area, explicitly state that you checked it and why it appears safe
- At the end, provide a concise “Security Scorecard” covering auth, secrets, input validation, API security, dependency security, frontend security, infrastructure, and abuse prevention


Do a full security hardening pass on this entire project. Audit the full codebase for vulnerabilities, fix every safe issue you can directly in code, add missing protections, tighten configs, and then give me a detailed report of what was found, what was changed, what remains risky, and what still needs manual decisions. Cover secrets, auth, authorization, sessions, JWTs, cookies, CSRF, XSS, SSRF, SQL/NoSQL injection, file uploads, input validation, rate limiting, CORS, security headers, dependency CVEs, logging leaks, error leakage, multi-tenant isolation, admin access, client-side exposure, infrastructure config, CI/CD, and business logic abuse. Preserve functionality and test everything after changes.

Start by scanning the repo and produce:
1. a security findings list
2. the fixes you plan to apply
3. the actual code changes
4. a final post-fix security report