## Packages
framer-motion | Smooth animations for stage transitions and card interactions
date-fns | Formatting timestamps (e.g., "2 minutes ago")
clsx | Utility for conditional classes (often used with tailwind-merge)
tailwind-merge | Utility for merging tailwind classes safely

## Notes
- Tailwind config should extend fonts for 'outfit' (headings) and 'inter' (body).
- Poll GET /api/queries/:id every 2s when status is 'processing'.
